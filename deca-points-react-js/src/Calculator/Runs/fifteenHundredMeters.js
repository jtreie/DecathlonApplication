const fifteenHundredMeters = {
    a : 0.03768,
    b : 480,
    c : 1.85
}
export default {fifteenHundredMeters};
        // double t = TimeSpan.Parse("0:"+time()).TotalSeconds;
        //     double p = a * Math.Pow((b - t), c);
        //     double points = Math.Floor(p);